using System.Collections.Generic;
using System.Dynamic;
using System.Linq;

using Dapper;

namespace AdminPannel
{
    public static class DapperExtensions
    {
        //public static IEnumerable<ExpandoObject> ToExpandoObjects(this IEnumerable<DapperRow> rows)
        //{
        //    return rows.Select(row => row.AsExpando());
        //}

        //public static ExpandoObject AsExpando(this Dapper. row)
        //{
        //    return row.ToDictionary(kvp => kvp.Key, kvp => kvp.Value) as ExpandoObject;
        //}
    }
}



